import React from 'react'

type Props = {}

const Filters = (props: Props) => {
    return (
        <section className="bg-red-600 row-span-full">
            <p>Filters</p>
        </section>
    )
}

export default Filters
